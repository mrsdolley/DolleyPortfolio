//Exercise One
var command = "Enter a number!";
var number = prompt(command);

var output = 0;

for (var num = 0; num <= number; num++) 
{output = output + num; 
}

alert(output);

//Excerise Two
var questionOne = "Do you want to play?";
prompt(questionOne);

var firstAnswer = "yes";

var command = "Enter a word";

// var questionTwo = "Do you want to play again?";

// var secondAnswer = "no";
// //var finalAnswer = 

// if(questionOne = firstAnswer) {prompt(command);} else { alert(secondAnswer);}


